#!/usr/bin/env ruby

require_relative 'lib/theme'
require_relative 'lib/prompt_modules'

theme = Theme.new
PromptModules.init(theme)

lines = []
lines << [ Path.new ]
lines << [ Cursor.new ]

lines.each do |line|
  puts line.join
end
