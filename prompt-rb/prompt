#!/usr/bin/env ruby

require_relative 'lib/theme'

@theme = Theme.new

def prompt_char
  exit_code = `echo $?`

  if exit_code && exit_code.to_i != 0
    color = @theme.red
  else
    color = @theme.blue
  end

  "#{color.to_24bit}❯#{Color.reset}"
end

puts "#{prompt_char}"
